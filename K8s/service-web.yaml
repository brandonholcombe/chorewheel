apiVersion: v1
kind: Service
metadata:
  name: chorewheel-web
  namespace: chorewheel
  labels:
    app: chorewheel-web
spec:
  type: ClusterIP
  selector:
    app: chorewheel-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
